import { DragDropSection } from './drag-drop-section.directive';

describe('DragDropSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropSection();
    expect(directive).toBeTruthy();
  });
});
