import { DragDropSectionItem } from './drag-drop-section-item.directive';

describe('DragDropSectionItemDirective', () => {
  it('should create an instance', () => {
  //  const directive = new DragDropSectionItem();
    // expect(directive).toBeTruthy();
  });
});
